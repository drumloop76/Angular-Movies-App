import { ModalTemplateDirective } from './modal-template.directive';

describe('ModalTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
