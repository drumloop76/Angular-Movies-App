import { SwiperCarouselDirective } from './swiper-carousel.directive';

describe('SwiperCarouselDirective', () => {
  it('should create an instance', () => {
    const directive = new SwiperCarouselDirective();
    expect(directive).toBeTruthy();
  });
});
