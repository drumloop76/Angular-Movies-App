import { CarouselBtnsDirective } from './carousel-btns.directive';

describe('CarouselBtnsDirective', () => {
  it('should create an instance', () => {
    const directive = new CarouselBtnsDirective();
    expect(directive).toBeTruthy();
  });
});
